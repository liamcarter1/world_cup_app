{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && (prisma db push --skip-generate --accept-data-loss || echo 'skip db push (no DATABASE_URL yet)') && next build",
  "crons": [
    {
      "path": "/api/cron/sync",
      "schedule": "*/10 * * * *"
    }
  ]
}
